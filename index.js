const app = require('./app');
// const { baseUrl, port } = require('./utils/constant');

app.listen(3000, () => {
    // console.log(`Server running at ${baseUrl}:${port}`);
    console.log("JALAN CUY!!! http://localhost:3000");
});
